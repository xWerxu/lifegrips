@extends('layouts.admin')

@section('content')
    jestes adminem jak cos
    {{-- @foreach ($test as $t)
        @foreach ($t->variants as $variant)
            {{ dd($variant->pivot->value) }}
        @endforeach
    @endforeach --}}
@endsection